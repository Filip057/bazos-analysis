# VCS
.git/
.gitignore
.gitattributes

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
venv/
env/
.venv/

# Local config / secrets
.env
.env.production
.env.local

# Logs and runtime artifacts
*.log
scraper.log
pipeline_checkpoints/
training_reports/
*.backup

# Local databases
*.db
*.sqlite
*.sqlite3

# Docs (not needed at runtime)
docs/
*.md
!README.md

# Tests (skip in production image)
tests/

# Local-only dev/ML utilities — never need to ship to production
dev_scripts/

# Note: scripts/ used to be excluded entirely, but it now contains
# create_admin.py which must be available inside the production image
# so admins can be provisioned via `railway ssh`. Keep it included.

# Training data (large local files)
training_*.json
filtered_*.json
*_texts.txt
extraction_stats.json
review_queue.json
manual_review_data.json
training_queue.json
error_analysis_report.json

# IDE / OS
.vscode/
.idea/
.DS_Store
nltk_data/
get-pip.py

# CSV exports / analysis dumps
*.csv
